MinHeightBst

for this problem, we can use the insert method. However this would give time complexity of O(nlog(n)) and space complexity of O(n)

By creating our own insert, we can minimize the time complexity to O(nlog(n)) and space complexity of O(n)